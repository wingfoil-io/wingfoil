# Generated by scripts/build-demo-vendor.sh — the demo's browser
# dependencies, served from this origin instead of a CDN.
#
# Build output, so it is not committed: the client half is compiled from
# `js/` in this repo and must track the commit it ships with, which a
# checked-in copy would silently stop doing. Both consumers of this
# directory run the script first — see the script's header.
vendor/
